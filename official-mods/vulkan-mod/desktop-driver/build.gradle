plugins {
    id 'java-library'
}

group = 'io.github.endx'
version = rootProject.componentVersions.vulkanMod

repositories {
    mavenCentral()
}

def lwjglVersion = project(':official-mods:vulkan-mod').lwjglVersion

dependencies {
    compileOnly files(project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs)
    implementation "org.lwjgl:lwjgl:${lwjglVersion}"
    implementation "org.lwjgl:lwjgl-vulkan:${lwjglVersion}"
    implementation "org.lwjgl:lwjgl-shaderc:${lwjglVersion}"
    runtimeOnly "org.lwjgl:lwjgl:${lwjglVersion}:natives-windows"
    runtimeOnly "org.lwjgl:lwjgl-shaderc:${lwjglVersion}:natives-windows"
}

tasks.withType(JavaCompile).configureEach {
    dependsOn project(':official-mods:vulkan-mod').tasks.named('compileJava')
    options.release = 13
}

jar {
    archiveBaseName.set('vulkan-mod-desktop-driver')
}

tasks.register('probeVulkan', JavaExec) {
    group = 'verification'
    description = 'Runs the standalone desktop Vulkan driver probe outside the LWJGL 2 game process.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneVulkanProbe')
}

tasks.register('verifyNativeReadback', JavaExec) {
    group = 'verification'
    description = 'Renders and reads back a small Vulkan target on the real desktop device.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneVulkanReadbackVerification')
}

tasks.register('verifyCustomShaderTranslation', JavaExec) {
    group = 'verification'
    description = 'Compiles translated GLSL-130 vertex/fragment input with isolated shaderc.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneCustomShaderVerification')
}

tasks.register('verifyLargeNativeBatchFrame', JavaExec) {
    group = 'verification'
    description = 'Presents 20,000 alternating batches without growing LWJGL MemoryStack per draw.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneLargeBatchVerification')
}

tasks.register('verifySpriteRunSubmission', JavaExec) {
    group = 'verification'
    description = 'Presents 12,000 transformed sprites as one recycled Java command and batch.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneSpriteRunVerification')
}

tasks.register('verifyDescriptorReuse', JavaExec) {
    group = 'verification'
    description = 'Verifies lazy, recycled descriptors and redundant texture-bind removal.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneDescriptorReuseVerification')
}

tasks.register('verifyCommandStateReuse', JavaExec) {
    group = 'verification'
    description = 'Verifies redundant Vulkan command-buffer state changes are omitted.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneCommandStateReuseVerification')
}

tasks.register('verifyMemoryAllocator', JavaExec) {
    group = 'verification'
    description = 'Verifies Vulkan memory ownership, persistent mapping, and image retirement.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneMemoryAllocatorVerification')
}

tasks.register('verifyImageResourceFactory', JavaExec) {
    group = 'verification'
    description = 'Verifies sampled/offscreen image creation and fence-safe retirement ownership.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneImageResourceVerification')
}

tasks.register('verifyCustomVertexPipeline', JavaExec) {
    group = 'verification'
    description = 'Executes a translated custom vertex shader on the real Vulkan device.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneCustomVertexPipelineVerification')
}

tasks.register('verifyAsyncOffscreenSubmissions', JavaExec) {
    group = 'verification'
    description = 'Queues dependent render-target passes without per-pass device/queue idle.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneAsyncOffscreenVerification')
}

tasks.register('verifyFrameGraphSubmission', JavaExec) {
    group = 'verification'
    description = 'Executes dependent offscreen passes and presentation in one queue submit.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneFrameGraphVerification')
}

tasks.register('verifyResourceStreamExecution', JavaExec) {
    group = 'verification'
    description = 'Executes typed logical texture lifecycle through ResourceStream and FrameStream.'
    classpath = files(
            project(':official-mods:vulkan-mod').sourceSets.main.output.classesDirs,
            sourceSets.main.runtimeClasspath)
    mainClass.set('io.github.endx.vulkanmod.lwjgl3.StandaloneResourceStreamVerification')
}
