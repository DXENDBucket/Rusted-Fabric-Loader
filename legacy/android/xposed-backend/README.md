# Frozen Xposed backend

These sources were extracted from the former combined Android application. They depend on the
other archived native-APK modules in `legacy/android` and are intentionally not an active,
standalone Gradle build. Git history before the launcher split contains the last integrated build.
