package com.corrodinggames.rts.appFramework;

import android.app.Application;

/**
 * Compile-time signature only. The real superclass is resolved from the user's APK at runtime.
 * This module is compileOnly and its class definition must never enter a distributed DEX.
 */
public class RWApplication extends Application {
}
