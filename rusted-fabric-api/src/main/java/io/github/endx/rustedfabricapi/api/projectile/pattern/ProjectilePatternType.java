package io.github.endx.rustedfabricapi.api.projectile.pattern;

/** Deterministic, non-recursive projectile layout primitives. */
public enum ProjectilePatternType {
    SINGLE,
    FAN,
    RING,
    LINE
}
